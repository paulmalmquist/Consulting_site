import type { Metadata } from 'next';

export const metadata: Metadata = {
  title: 'Capabilities',
  description: 'Core capabilities for dependency reduction, replacement execution, and governance-safe modernization.'
};
